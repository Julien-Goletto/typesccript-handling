let str = "Hello World";

str = 10; // le type est assignés à la déclaration

// Compilation avec la commande tsc script.ts à la racine du folder
// tsc --init création du fichier tsconfig.json
// utiliser le watcher tsc -w

const foo = () => "Hello World";
console.log(foo());
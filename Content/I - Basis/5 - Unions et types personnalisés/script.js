"use strict";
// Unions
let code; // Première union de typage
code = 5;
code = "six";
code = false;
let arr;
arr = [true, false, 6];
const foo = (param) => console.log(param);
foo('test');
const baz = (param) => console.log(param);
foo('test');
``;
let test = true;
test = { a: "Toto" };
const firstButton = {
    x: 10,
    y: 74,
    id: 'a298',
    visible: true,
};
